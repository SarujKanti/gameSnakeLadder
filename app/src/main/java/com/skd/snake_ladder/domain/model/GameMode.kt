package com.skd.snake_ladder.domain.model

enum class GameMode {
    VS_COMPUTER,
    MULTI_PLAYER,  // 2 – 6 human players on the same device
    ONLINE         // 2 – 6 players over the internet via Firebase
}
